import { consumeGuestLink, isTrustedMutation, sessionCookie } from "../../../../../src/server/auth";
import { runtimeEnv } from "../../../../../src/server/runtime";
export async function GET(request:Request,{params}:{params:Promise<{token:string}>}){const env=await runtimeEnv(),token=(await params).token,base=env.AUTH_BASE_URL??request.url;return Response.redirect(new URL(`/guest/${encodeURIComponent(token)}`,base))}
export async function POST(request:Request,{params}:{params:Promise<{token:string}>}){try{const env=await runtimeEnv();if(!isTrustedMutation(request,env.AUTH_BASE_URL))return Response.json({error:"Cross-origin mutation denied"},{status:403});if(!env.DB)return Response.json({error:"Database is not configured"},{status:503});const result=await consumeGuestLink(env.DB,env,(await params).token,request),base=env.AUTH_BASE_URL??request.url;return new Response(null,{status:302,headers:{location:new URL(`/?workspace=${encodeURIComponent(result.workspaceId)}`,base).toString(),"set-cookie":sessionCookie(result.session.raw,result.session.maxAge),"cache-control":"no-store"}})}catch(error){return Response.json({error:error instanceof Error?error.message:"Guest sign-in failed"},{status:401})}}
