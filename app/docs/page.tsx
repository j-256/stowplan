import Link from "next/link";
export default function InAppDocs(){return <main className="admin-page"><header><div><p className="eyebrow">Offline quick guide</p><h1>Using Stowplan</h1></div><Link href="/">Back to organizer</Link></header><section><h2>1. Capture the physical truth</h2><p>Label a space, enter quantity + unit + item, add every nested container while you are looking at it, then mark the space counted. Use known empty only after checking it.</p></section><section><h2>2. Keep working offline</h2><p>Accepted changes are saved on this device before backup. “Saved on this device” is safe; “Up to date” means the server has also accepted the queue. Export before clearing browser data.</p></section><section><h2>3. Organize and plan</h2><p>Add location conditions and item constraints, search and bulk-move records, then generate an explainable plan. Whole boxes can move as a single step.</p></section><section><h2>4. Correct mistakes safely</h2><p>Activity can undo or reapply one selected change, or undo/redo the latest N. Same-field conflicts stop for review instead of overwriting newer data.</p></section><section><h2>Full documentation</h2><p>The complete user, deployment, authentication, recovery, and maintainer guides are hosted independently on GitHub Pages and are available from the source repository.</p><a href={process.env.NEXT_PUBLIC_DOCS_URL || "https://j-256.github.io/stowplan/"}>Open full documentation</a></section></main>}
