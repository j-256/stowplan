# Stowplan agent guide

The complete agent handoff is in `docs/maintainers/agents.md`. Preserve local-first durability, field-aware history/conflicts, workspace-scoped authorization, runtime-neutral domain code, and mobile-first capture. Run focused tests while implementing. After the final commit, run `npm run verify:ready`; do not claim the work is done or ready to push until that exact `HEAD` passes.
