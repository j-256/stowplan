interface:
  display_name: "Release Stowplan"
  short_description: "Publish Stowplan to GitHub and Sites"
  default_prompt: "Use $stowplan-release to verify, publish a GitHub release, deploy Sites, and smoke-test the current Stowplan main branch."
policy:
  allow_implicit_invocation: false
