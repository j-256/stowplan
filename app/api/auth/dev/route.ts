import { createOrLinkUser, isTrustedMutation, issueSession, sessionCookie } from "../../../../src/server/auth";
import { runtimeEnv } from "../../../../src/server/runtime";
export async function POST(request:Request){if(!isTrustedMutation(request))return Response.json({error:"Cross-origin mutation denied"},{status:403});const env=await runtimeEnv();if(!env.DB)return Response.json({error:"Database is not configured"},{status:503});if(env.AUTH_DEV_ENABLED!=="true"&&process.env.NODE_ENV==="production")return Response.json({error:"Development authentication is disabled"},{status:404});const body=await request.json() as {email?:string;name?:string};const email=body.email?.trim().toLowerCase();if(!email)return Response.json({error:"Email is required"},{status:400});const user=await createOrLinkUser(env.DB,env,{provider:"development",subject:email,email,displayName:body.name?.trim()||email});const session=await issueSession(env.DB,env,user,request);return Response.json({user},{headers:{"set-cookie":sessionCookie(session.raw,session.maxAge),"cache-control":"no-store"}})}
